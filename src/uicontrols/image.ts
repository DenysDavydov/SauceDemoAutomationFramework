import { SearchCriteria } from '../utils/search-criteria';
import { UiElement } from './uielement';

export class Image extends UiElement {
    // async getBitmap(searchCriteria: SearchCriteria = {}): Promise<ImageBitmap> {
    //     const element = await this.find(searchCriteria).evaluate(e => e as HTMLImageElement);
    //     return await createImageBitmap(element);
    // }
}